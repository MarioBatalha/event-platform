import "./chunk-5CYG367S.js";

// node_modules/data-fns/dist/data-fns.esm.js
var getItemCyclic = function getItemCyclic2(index, array) {
  var id = index % array.length;
  return array[id];
};
var offset = function offset2(list, amount) {
  return [].concat(list.slice(amount), list.slice(0, amount));
};
var patternChunks = function patternChunks2(array, pattern) {
  var result = [];
  var _array = [].concat(array);
  var i = 0;
  while (_array.length > 0) {
    result.push(_array.splice(0, getItemCyclic(i, pattern)));
    i++;
  }
  return result;
};
var replaceAt = function replaceAt2(array, index, replacementFn) {
  var item = array[index];
  return array.slice(0, index).concat([replacementFn(item)]).concat(array.slice(index + 1));
};
var times = function times2(iterations, callback) {
  var result = [];
  for (var i = 0; i < iterations; i++) {
    result.push(callback(i));
  }
  return result;
};
export {
  getItemCyclic,
  offset,
  patternChunks,
  replaceAt,
  times
};
//# sourceMappingURL=data-fns.js.map
